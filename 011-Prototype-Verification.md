---
title: 플레이 & 기능 검증 (Prototype & Verification) 가이드라인
description: 핵심 재미·로직·기술을 빠르게 검증하기 위한 산출물 종별, 웹/LLM 프로토타입 방법, 판정선·계측·재검증 체크리스트를 제시하는 가이드
---

**플레이 & 기능 검증 (Prototype & Verification) 가이드라인**
1. **목적** : 핵심 재미·기술·데이터 구조 리스크를 저비용으로 사전 검증하기 위한 목적을 정의.
2. **범위** : Prototype·First Playable·Vertical Slice·MVP·Demo의 구분과 웹/LLM 프로토타입, 핵심 메카닉 단위 테스트의 포함·제외 범위를 명시.
3. **검증 목표** : 재미·논리·기술 3대 검증 목표를 정성/정량 관점에서 설정.
4. **프로토타이핑 방법론** : 산출물 종별 선택, 웹/LLM 간소 구현, 프롬프트 고도화로 빠른 제작·검증 흐름 정리.
5. **단계별 검증 체크리스트** : Pre-Alpha~기술/데이터 단계별로 검증할 항목을 체크리스트로 제시.
6. **산출물** : 종별·빌드 ID가 적힌 플레이 가능 빌드, 텔레메트리 이벤트 정의, 세션 규약, 재검증 표본, 개선·중단 제안서 5종.

---

# 1. 목적
게임 개발의 초기 단계에서 핵심 재미, 기술적 실현 가능성, 데이터 구조의 안정성을 빠르고 저비용으로 검증하기 위한 가이드입니다. 본격적인 프로덕션(Production) 진입 전 리스크를 최소화하는 것을 목표로 합니다.

# 2. 범위
- **포함**: 웹 기반 프로토타입(HTML/JS), LLM 활용 동작 검증, 산출물 종별 테스트 계획, 핵심 메카닉 단위 테스트
- **제외**: 상용 엔진(Unity/Unreal) 최적화, 최종 아트 리소스 제작, 마케팅/비즈니스 검증

# 3. 검증 목표

1.  **재미 검증**: 기획한 규칙이 실제로 플레이했을 때 재미있는가? (정성적 평가)
2.  **논리 검증**: 데이터 구조와 알고리즘이 모순 없이 작동하는가? (정량적/TDD)
3.  **기술 검증**: 핵심 메커니즘이 목표 플랫폼/환경에서 구현 가능한가?

# 4. 프로토타이핑 방법론

## 4-1. 웹/LLM 간소 구현

- 상용 엔진을 켜기 전, HTML/JS와 LLM을 활용해 핵심 로직을 텍스트/웹상에서 즉시 구현하고 테스트합니다.

- **도구**: Claude, ChatGPT, Gemini, Cursor, v0.dev, Replit
- **방식**:
  1. **프롬프트 입력**: 기획서(마크다운) 내용을 LLM에 입력하여 "이 규칙으로 동작하는 HTML 미니게임을 만들어줘"라고 요청.
  2. **데이터 연동(선택사항)**: json 을 연동하도록 구성해서 동작 확인
  3. **반복 개선**: 플레이 후 "공격 속도가 너무 느려", "UI가 복잡해" 등의 피드백을 주어 즉시 수정.

## 4-2. 산출물 종별 선택과 LLM 기반 얇은 빌드

- 착수 전에 아래 다섯 종 중 **정확히 하나**를 고릅니다. `Prototype / MVP`처럼 슬래시로 묶지 않습니다.
  - **Prototype**: 질문 하나에 답하는 단일 시스템의 플레이 가능 예. 최종 아트·전체 흐름·출시 가능성은 의도적으로 제외합니다.
  - **First Playable**: 처음 보는 외부인이 상용 제품의 한 레벨로 믿을 품질의 첫 플레이 구간. 핵심 경험과 아트 방향이 함께 있어야 합니다.
  - **Vertical Slice**: 계획된 주요 시스템과 제작 파이프라인이 대표 구간 하나에서 함께 작동하는 프로덕션 기준 샘플입니다.
  - **MVP**: 사용자 가치가 처음부터 끝까지 성립하는 critical path minimum. 이 워크플로우의 웹 MVP는 보통 `진입 → 플레이 → 결과`만 잇고 주변 시스템은 뺍니다.
  - **Demo**: 구매·행사·외부 피드백을 위한 관객용 발췌본. 안정성·온보딩·이후 콘텐츠 예고를 포함합니다.
- 계획 첫 줄에 `종별 / 답할 질문 / 관찰자 / 요구 완성도 / 포함 / 의도적 제외 / 매체 / 기간 상한 / 중단 조건`을 적습니다. 서로 다른 종별을 동시에 요구하면 파일과 일정부터 분리합니다.
- 아래 예시는 LLM의 컨텍스트 한계를 고려한 **웹 Prototype 또는 MVP**입니다. 흐름(Flow)과 모듈(Module)을 나누어 만들며 First Playable·Vertical Slice·Demo의 품질 판정에는 그대로 사용하지 않습니다.

**예시 A:** 전체 스크린 플로우 검증 (Flow Slice)

- 단일 HTML 파일 내에서 버튼/UI 인터랙션만으로 게임의 전체 진입부터 종료까지의 흐름을 검증합니다. 실제 게임 플레이 로직은 생략하거나 단순화합니다.

- **목적**: UI 구조, 화면 전환 UX, 메뉴 진입 동선 검증
- **프롬프트 예시**:

    ```
    다음 레이싱 게임의 스크린 플로우를 단일 HTML로 구현해줘. 실제 레이싱 게임 플레이는 생략하고,
    각 단계는 버튼 클릭으로 시뮬레이션해줘. 핵심은 전체 화면 전환 흐름을 검증하는 거야.
    1.  **타이틀** -> **로비**
    2.  **로비 구조**: [리그 참여], [차고/정비], [옵션] 메뉴 존재.
    3.  **리그 참여** -> [리그 리스트] -> [참여] -> [레이싱(생략/결과 바로가기)] -> [결과 화면] -> **로비** 복귀.
    4.  **차고** -> [차량 선택] -> [업그레이드] -> **로비** 복귀.
    5.  **옵션** -> [사운드 설정] -> **로비** 복귀.
    각 화면은 간단한 와이어프레임 박스 형태로 레이아웃만 잡아주고, 뒤로 가기 및 홈 이동 버튼을 배치해줘.
    ```

**예시 B**: 특정 기능 모듈 검증 (Module Slice)

- 복잡한 게임 전체를 만드는 대신, "강화", "전투", "상점" 등 특정 핵심 기능 하나만 떼어내어 깊이 있게 구현하고 조작감과 밸런스를 검증합니다.

- **목적**: 핵심 재미 요소(Core Fun), 조작감, 데이터 구조(JSON) 활용
- **프롬프트 예시 1**: 기능 모듈
    ```
    1280x768 해상도의 PC플랫폼 레이싱 게임의 '차량 업그레이드' 기능만 따로 떼어서 단일 HTML 프로토타입으로 만들어줘.

    **[핵심 요구사항: 데이터 기반 수정 기능]**
    1. 모든 수치(자금, 파츠 가격, 스탯)는 자바스크립트 내 `config` 객체로 분리해줘.
    2. 화면 우측 하단에 **[Config 입력창]**(TextArea)을 만들어서, JSON 텍스트를 붙여넣고
       [적용] 버튼을 누르면 즉시 게임 내 밸런스가 업데이트되게 해줘. (CORS 문제 방지)
    3. 기본 데이터 포맷 예시:
       {
         "initialMoney": 10000,
         "parts": [
           { "id": "engine", "name": "Engine", "basePrice": 1000, "priceMult": 1.5, "stat": "power" }
         ]
       }

    **[기능 명세]**
    - 부품 구매 시 자금 차감 및 스탯 즉시 반영.
    - 다른 화면 불필요. 오직 업그레이드 패널 테스트에 집중.
    ```

**예시 C**: 핵심 메카닉 검증 (Core Mechanics)
- **목적**: 기본/핵심 동작을 점검
- **프롬프트 예시 1**: 핵심 메카닉 (Idle Game)

    ```
    다음 탭 아이들(Clicker) 게임의 핵심 메카닉을 검증하기 위한 단일 HTML 프로토타입을 만들어줘.
    1. **기본 동작**: 화면 중앙 버튼을 탭하면 +1 골드 획득.
    2. **업그레이드 1 (탭 강화)**: "탭 당 획득량 +1" 기능. 비용은 10 골드부터 시작하여 구매 시 1.5배씩 증가.
    3. **업그레이드 2 (자동 수익)**: "1초당 자동 획득 +1" 기능. 비용은 50 골드부터 시작하여 구매 시 1.5배씩 증가.
    4. **업그레이드 3 (고급 자동 수익)**: "1초당 자동 획득 +10" 기능. 비용은 500 골드부터 시작.
    5. **UI 레이아웃**: 상단에 [현재 골드], 중앙에 [탭 버튼], 하단에 [업그레이드 리스트] 배치.
    밸런스는 대략적이어도 좋으니, '탭 -> 골드 수집 -> 업그레이드 -> 더 빠른 수집'의 루프가 정상 작동하는지 확인하고 싶어.
    ```
## 4-3. LLM 프롬프트 고도화 (Prompt Engineering)

- LLM으로 프롬프팅을 생성할 경우 주요 기능외의 세부사항 프롬프팅 작성 시간을 절약할 수 있습니다.

- **방식**:
  1. **초안 작성**: "레이싱 게임 업그레이드 화면 만들어줘" 정도로 간단히 작성.
  2. **프롬프트 강화 요청**:
      ```
      지금 내가 [레이싱 게임 업그레이드 화면]을 HTML로 즉시 구현하려 해.
      개발용 LLM에게 입력할 **구체적이고 명확한 지시 프롬프트**를 대신 작성해줘.
      해상도, 필수 기능, 예외 처리를 포함해서.
      ```
  3.  **검토 및 수정**: LLM이 작성한 프롬프트에서 불필요한 부분(과도한 그래픽 요구 등)을 삭제하고, '단일 HTML 파일', '와이어프레임 스타일' 등 제약 조건을 추가.
  4.  **최종 생성**: 수정된 프롬프트를 사용하여 프로토타입 생성 요청.

## 4-4. 프로토타입 설계 원칙 (What to Prototype)

- **하나의 질문에만 답한다**: "재미있는가", "만들 수 있는가", "사람들이 원하는가" 중 하나를 정하고 그 검증에만 집중합니다. 질문이 2개 이상이면 프로토타입을 분리합니다(게임플레이/기술/내러티브 프로토타입은 프로젝트 파일부터 분리).
- **매체가 답할 수 없는 질문을 먼저 적는다**: 웹·HTML은 규칙 이해·클릭 경로·상태 보존·데이터 로딩에 적합하지만 입력 지연·프레임타임·최소 사양·네이티브 플랫폼 기능을 왜곡합니다. 후자는 목표 엔진·목표 기기의 별도 빌드로 검증하며, 종이·스프레드시트는 카드·경제·확률·턴 규칙에 씁니다. 계획서에 `이 매체로 답할 수 없는 질문`이 없으면 매체 선택이 끝나지 않은 것입니다.
- **의도적 제외 목록을 쓴다**: 뺀 기능·아트·데이터를 고유명사로 적고, 제외 때문에 판정할 수 없게 된 질문도 같은 줄에 붙입니다. 만들 것만 적은 계획은 축소 제품이지 프로토타입이 아닙니다.
- **MVP 판별 기준은 "이 기능을 잘라도 성립하는가"**: Super Mario Bros.의 MVP는 이동·점프·구덩이 3요소뿐입니다(굼바·버섯·파이어플라워 전부 제외). 달리고 구덩이를 넘는 것이 재미없으면 어떤 기능을 얹어도 성립하지 않습니다. 콘텐츠(적 종류·레벨·무기)는 걷어냅니다 — 콘텐츠가 많으면 테스터가 나쁜 콘텐츠를 지적하느라 기반 문제를 진단할 수 없습니다.
- **쉬운 부분이 아니라 훅과 리스크를 증명한다**: "캐릭터가 걷고 문을 연다"는 프로토는 아무것도 증명하지 못합니다. Portal이라면 포털 메커닉만 검증하고 나머지 FPS 관습은 기존 것을 그대로 씁니다.
- **남의 게임 위에 얹어 0원으로 검증한다**: MARVEL SNAP의 Ben Brode는 백개먼의 더블링 큐브 규칙을 Hearthstone 대전 중 구두 합의만으로 검증했습니다(코어 룰 확정 2일). 규칙 검증에는 코드가 필요 없는 경우가 많습니다 — 종이 목업·스프레드시트도 유효한 프로토타입입니다.
- **기간 상한을 강제한다**: Respawn의 액션 블록 규칙 — 기본 1주, 하드캡 2주, 실패작 포함 전부 팀에 공유(6주에 45개 제작, "실패를 서랍에 감추면 남들이 반복한다"). 며칠을 반복해도 '겨우 괜찮음(fine)' 수준이면 6개월을 더 투자할 가치를 의심합니다.
- **취소 조건을 두 갈래로 쓴다**: `기간 안에 판정 가능한 빌드를 만들지 못함`과 `빌드는 만들었지만 계속 투자할 가치가 없음`을 각각 관찰 가능한 문장으로 적습니다. 실패 시 버릴 대상은 `컨셉 재검토`가 아니라 기능·규칙·매체 이름이어야 합니다.
- **절반은 재미없어야 정상**: 프로토타입의 가치는 성공이 아니라 "2년 낭비를 몇 주로 줄이는 것"입니다 (Fruit Ninja의 Luke Muscat: "커리어에서 만든 프로토타입의 최소 절반은 전혀 재미없었다").
- **그레이박스 원칙과 예외**: 아트·사운드·UI를 넣지 않는 것이 기본입니다. 단 타격감·게임 필 자체가 핵심 가설인 게임은 최소한의 파티클·이펙트 없이는 재미를 오판합니다 (Gears of War의 커버 시스템은 이펙트를 붙이기 전까지 팀 내부에서도 팔리지 않았습니다). 이 예외 적용 여부를 프로토 계획에 명시합니다.
- **데모/버티컬 슬라이스에는 '이후 예고'를 포함한다**: 데모 1시간이 게임 전체를 드러내면 구매 차단 장치가 됩니다(Sugardew Island 사례). 슬라이스 범위는 기능 나열이 아니라 코어 루프 1회전 완주 기준으로 자릅니다.

# 5. 검증 축 체크리스트 (Verification Axes)

- 아래 모든 체크 항목은 **(판정선 / 계측 수단 / 그 수단이 선언된 위치)** 세 값을 같은 줄에 적는다. 계측 수단은 §6 텔레메트리 이벤트 정의에 **실재하는 이벤트·필드명**이거나 영상 구간이어야 하며, 정의에 없는 이름을 가리킨 항목은 통과가 아니라 **미판정**으로 센다.
- [ ] **산출물 종별**: 다섯 종 중 하나, 의도적 제외 목록, 기간 상한, 두 갈래 취소 조건이 첫 줄에 있는가?
- [ ] **화면 상태 커버율**: 화면 수 `S`와 정상·로딩·빈·오류가 정의된 칸 수 `D`를 세어 `D ÷ (S×4)`를 적었는가? 오류마다 복귀 화면이 이름으로 있고, 중단·재개 시 보존·폐기 값이 있는가?

## 축 A. 로직 및 아이디어 (Pre-Alpha)
- [ ] **핵심 루프 작동**: "탐험 -> 전투 -> 보상 -> 성장"의 사이클이 끊김 없이 도는가?
- [ ] **엣지 케이스 처리**: 자원 부족, 인벤토리 가득 참, 시간 초과 등 예외 상황에서 게임이 멈추지 않는가?
- [ ] **확률/밸런스**: 시뮬레이션 결과가 기획 의도(승률, 자원 수급량)와 오차 범위(±10%) 내인가?

- [ ] **검증 질문 단일화**: 이 프로토타입이 답하려는 질문이 1개로 정의되었는가?

## 축 B. 플레이 경험 (Alpha Prototype)
- [ ] **조작감**: 목표 엔진·목표 기기 빌드라면 입력 지연 판정선과 계측 수단이 있는가? 웹·HTML 빌드라면 `미판정 — 목표 엔진 빌드에서 검증`으로 남겼는가?
- [ ] **학습 난이도**: 설명서 없이 플레이어가 첫 5분 안에 게임 규칙을 이해하는가? — "N명 중 M명"의 N을 아래 **세션 규약** 산출물로 먼저 확정한 뒤에만 채점한다.
- [ ] **Fun Factor**: 재도전 행동(이벤트로 셀 수 있는 것)으로 판정한다. 발화("한 판 더")를 근거로 쓰려면 그것을 유도하지 않을 진행자 규칙과 사후 질문 문장을 계획서에 먼저 적고, 없으면 미판정으로 남긴다.

- [ ] **감정 기준 통과**: 판정선이 기능 완성이 아니라 목표 감정의 재현으로 정의되었는가? (Subnautica는 모듈러 잠수함 조종을 '지루함'으로 폐기하고, 침수 경보·수리를 '폭력 없는 긴장'으로 채택)

## 축 C. 기술 및 데이터 구조
- [ ] **데이터 호환**: 기획서의 데이터(JSON/YAML)가 실제 코드에서 별도 변환 없이 로드되는가?
- [ ] **성능 목표**: 목표 엔진·목표 기기 빌드에서 최소 사양·프레임타임 판정선을 검증했는가? 웹·HTML 빌드의 결과를 네이티브 성능 통과 근거로 쓰지 않았는가?

# 6. 산출물 (Deliverables)

- **재생 가능한 빌드·데모 링크**: 선택한 종별과 빌드ID를 붙인 실행 파일·영상·URL. 웹 산출물은 클릭 한 번으로 실행 가능한 HTML 파일 또는 배포 URL을 씁니다.
- **텔레메트리 이벤트 정의**: 이벤트 1개 = **이름 문자열 + 필드 목록 + 값이 채워진 페이로드 예시 1행**. 셋 중 하나라도 없으면 산출물로 세지 않는다 — 필드 이름만 나열한 스키마 선언은 아무 값도 산출하지 않는다(실물 기준: `enemyDefeated:boss:dragon` + `value=14`, `progression01=world_1, progressionStatus=complete, value=92`).
    - 시간·점수·좌표·ID 같은 동적 값은 이름이 아니라 필드에 넣는다(`stage1_boss_died_at_x120` 형태 금지). 열거형 필드는 취할 수 있는 값 집합을 같은 줄에 적고, 진행 단위마다 `start / complete / fail` 3상태를 정의한다 — 없으면 어디서 이탈했는지 영영 계산할 수 없다.
    - 이벤트마다 그것이 계산하는 §5 체크 항목을 적고, 역으로 **대응 이벤트가 없는 판정선은 판정선이 아니므로 미판정으로 남긴다.** 빌드ID·엔진 경고·assert 같은 개발 계측이 0건이면 게임플레이 절반만 계측한 것이다.
- **세션 규약**: 세션 1회의 정의 — 참가자 수와 출처(내부/외부), 세션 길이 상한(분), 개인정보·녹화·로그 수집 동의 절차, 진행자 발화·개입 규칙은 [231 §8](231-qa_plan_test_cases.md)을 그대로 인용해 붙이고 재정의하지 않는다, think-aloud 지시문 원문, 사후 질문 문장을 중요도 순으로, 분석 담당자와 분석 완료 시점. 규약 없이 모은 로그와 발화는 산출물로 세지 않는다.
- **텔레메트리 집계 + 재검증 표본**: 플레이 타임·승률·선택 분포와 함께 `화면 상태 커버율 D/(S×4)`·`판정선 대응률 E/K`를 적습니다. 수정 1건이 다음 참가자 투입 전까지 반영되는 시간 상한을 시간 단위로, 마지막 수정 이후 변경 없이 돌릴 인원 N명을 숫자로 적고, `영향을 준 발견 수/전체 발견 수`와 `재수정 수/전체 수정 수`를 기록합니다. 재검증 표본이 없으면 "고쳤다"는 주장입니다.
- **개선·중단 제안서**: 결과에 기반한 규칙·수치 변경뿐 아니라 `유지 / 별도 매체 재검증 / 기능 폐기 / 빌드 중단` 판정과 근거를 이름으로 적습니다.

# 관련 문서 (See Also)

- [QA 계획 & 테스트 케이스](231-qa_plan_test_cases.md) : §8의 진행자 발화 규칙("허용되는 한마디는 '아직 미완성이에요'뿐")·"의견을 듣지 말고 관찰하라"와 §5의 클리넥스 테스터 정의 — 세션 규약 문장은 여기가 단일 기준이며 이 문서에서 재정의하지 않는다.
- [텍스트·데이터 연동](001-Text-Data-Integration.md) : 프로토타입용 더미 데이터·수치 시뮬레이터 생성은 §4-2·§4-3이 단일 기준이다.
- [기획 산출물 평가 루브릭](012-Planning-Evaluation-Rubrics.md) : Phase 3 게이트·화면 상태 커버율·판정선 대응률·매체 적합성 판정의 단일 기준.
- [기획 가이드 근거 출처 대장](013-Planning-Evidence-Sources.md) : Valve·RITE·Cerny·GameAnalytics 등 이 가이드에 이식한 규칙의 영구 출처 대장.
